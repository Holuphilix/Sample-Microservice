Tests
-----

This directory will contain unit and integration tests.

Suggested tools:
- `jest` or `mocha` for unit tests
- `supertest` for HTTP integration tests

Structure:
- `tests/unit/`
- `tests/integration/`
